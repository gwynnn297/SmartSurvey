# Local overrides (do not commit your secrets)
# Copy this file to application-local.properties and edit values for your machine.
# This file is loaded automatically if present (see spring.config.import in application.properties)

# --- Database ---
DB_USERNAME=root
DB_PASSWORD=your_local_mysql_password
MYSQL_HOST=localhost

# --- JWT (optional override) ---
# JWT_SECRET=change_me_for_local
