# Gemini API Configuration
# New API key (December 17, 2025)
GEMINI_API_KEY=your_new_api_key_here

# Primary model - will auto-fallback to other models on rate limit
GEMINI_MODEL=gemini-2.5-flash

# Fallback chain (auto-switch when hitting rate limit):
# 1. gemini-2.5-flash (best quality, 6/5 RPM - over limit)
# 2. gemini-2.5-flash-lite (faster, 9/10 RPM - available)
# Note: Free tier only supports v2.5 models

# Service Configuration
AI_SERVICE_PORT=8002
LOG_LEVEL=INFO